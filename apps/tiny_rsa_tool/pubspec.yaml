name: tiny_rsa_tool
description: RSA encryption and decryption tool.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.1+4

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  asn1lib: ^1.5.2
  pointycastle: ^3.7.4
  url_launcher: ^6.2.5
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
